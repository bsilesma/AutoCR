package com.autocr;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Clase principal del proyecto AutoCR Pro.
 * Punto de entrada de la aplicacion Spring Boot (ejecutar desde NetBeans con "Run").
 */
@SpringBootApplication
public class AutocrApplication {

    public static void main(String[] args) {
        SpringApplication.run(AutocrApplication.class, args);
    }

}
